<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Calculadora de Inclinação</title>
    <link rel="stylesheet" href="/styles.css"> <!-- Link para o arquivo CSS -->
</head>
<body>
    <div class="container">
        <h1>Calculadora de Inclinação</h1>
        <form action="/calcular" method="POST" enctype="multipart/form-data">
            <label for="fileX">Arquivo X:</label>
            <input type="file" name="fileX" id="fileX" required><br><br>
            <label for="fileY">Arquivo Y:</label>
            <input type="file" name="fileY" id="fileY" required><br><br>
            <button type="submit">Calcular</button>
        </form>

        <% if (resultArredondado !== null) { %>
            <h2>Resultado Arredondado: <strong><%= resultArredondado %></strong></h2>
        <% } %>

        <% if (resultTruncado !== null) { %>
            <h2>Resultado Truncado: <strong><%= resultTruncado %></strong></h2>
        <% } %>

        <% if (typeof error !== 'undefined') { %>
            <p class="error"><%= error %></p>
        <% } %>
    </div>
</body>
</html>
